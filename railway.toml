[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No cron needed - the bot has its own timezone-aware scheduler
# that handles DST automatically (Europe/Brussels).
# Container stays running and triggers jobs at the configured time.
restartPolicyType = "ON_FAILURE"
